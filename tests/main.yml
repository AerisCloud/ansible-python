--- # Generated by ansible-role-test 0.1.0
name: "Installation test"
playbook:
- hosts: all
  sudo: true
  roles:
# "@ROLE_NAME@" is a magic variable that will be replaced by the name of the role
# on your filesystem before running the tests
  - role: "@ROLE_NAME@"
    python_packages:
    - pygments
    - { name: humanize, version: 0.5 }
  tasks:
  - name: "Check that the pygmentize exists"
    stat: path=/usr/bin/pygmentize
